# ─── Resend (contact form email) — server-side only, set in Vercel ───
#
# TESTING (no domain verified yet):
#   RESEND_API_KEY     = your re_... key from resend.com
#   RESEND_FROM_EMAIL  = onboarding@resend.dev
#   CONTACT_TO_EMAIL   = the SAME email you signed up with on Resend
#                        (sandbox only delivers to that inbox — not vikna@... yet)
#
# PRODUCTION (after scalingtheory.com is verified in Resend):
#   RESEND_FROM_EMAIL  = Scaling Theory <noreply@scalingtheory.com>
#   CONTACT_TO_EMAIL   = vikna@scalingtheory.com
#
# After changing vars → Redeploy on Vercel
# Local API testing: run `npx vercel dev` (not plain `npm run dev`)

RESEND_API_KEY=
RESEND_FROM_EMAIL=onboarding@resend.dev
CONTACT_TO_EMAIL=your-resend-signup-email@example.com
